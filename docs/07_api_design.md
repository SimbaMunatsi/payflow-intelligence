# 07. API Design

## Endpoints
- GET /health
- GET /transactions
- GET /accounts
- GET /summary

## Request/Response Notes
- Use JSON payloads
- Support pagination for large result sets
- Return clear error messages

## Authentication
- API keys or token-based auth can be introduced later
